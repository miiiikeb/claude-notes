{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "git clone --depth=1 --filter=blob:none --sparse https://x-access-token:${GH_TOKEN}@github.com/miiiikeb/claude-webAppStdPlatform /tmp/std-platform 2>/dev/null && git -C /tmp/std-platform sparse-checkout set .claude/skills && cp -r /tmp/std-platform/.claude/skills/. ~/.claude/skills/"
          }
        ]
      }
    ]
  }
}
